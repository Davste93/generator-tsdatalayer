"use strict";
var tsmvc_1 = require("tsmvc");
model.name %  > ;
from;
"../models/<%= model.name %>";
//Linked Resources
 % -strImports %  >
;
interface < ;
model.name %  > tsmvc_1.DataRepository;
tsmvc_1.DataRepository << ;
model.name %  >> {};
